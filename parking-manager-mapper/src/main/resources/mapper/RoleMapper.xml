<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="parking.manager.mapper.RoleMapper">
    <select id="getRolesByUserName" resultType="role">
    select c.roleId,c.roleName from sys_user a
    LEFT JOIN sys_user_role b on a.userId = b.uId
    LEFT JOIN sys_role c on b.rId =c.roleId
    where a.userName=#{userName}
    </select>

</mapper>